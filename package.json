{
  "name": "playground",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "install": "cd ui && npm install",
    "build": "cd lib && cargo build --release",
    "start": "npm run build && cd ui && npm start",
    "clean": "cd lib && cargo clean && rm -rf ./index.node && cd ../ui && npm run clean"
  },
  "author": "",
  "license": "ISC"
}
